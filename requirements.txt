# Vercel Python Serverless Functions 依赖
# 方案 B：全 Vercel 部署时使用
#
# 注意: Vercel 会自动从项目根目录的 requirements.txt 读取依赖。
# 此文件与 server/requirements.txt 内容一致，确保 Vercel 能正确安装。

fastapi==0.111.1
uvicorn[standard]==0.30.3
sqlalchemy==2.0.31
greenlet==3.0.3
pydantic==2.8.2
pydantic-settings==2.4.0
bcrypt==4.2.0
PyJWT==2.9.0
python-dotenv==1.0.1
python-multipart==0.0.9
passlib==1.7.4
httpx==0.27.0
asyncpg==0.29.0
